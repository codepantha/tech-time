import React from 'react';
import { Navbar } from '../components';

const Header = () => (
  <div className="h-screen max-w-full bg-primary px-4 md:px-20">
    <Navbar />
  </div>
);

export default Header;
